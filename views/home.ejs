<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Prisma Vault</title>

    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="styles.css" />
  </head>
  <body>
    <% if (user) { %> <%- include('inHeader') %> <% } else { %> <%-
    include('header') %> <% } %>

    <div class="container">
      <div class="wrapper">
        <div class="home-header">
          <% if (user) { %>
            <input type="text" value="/Technology/Web Development/Prisma Vault" class="form-control" readonly />
          <p>
            <span
              ><img class="header-logo" src="/images/crystal.png" alt=""
            /></span>
            <%= user.name %>
          </p>
          <% } else { %>
          <h2>Welcome</h2>
          <p>
            <em
              >You are currently not logged in. Please log in to get
              started.</em
            >
          </p>
          <div id="header-description">
            <strong>Prisma Vault</strong>
            <p>A stripped down version of Google Drive</p>
          </div>
          <% } %>

          <hr />
        </div>

        <% if (user) { %> <%- include('userActions') %> <% } %> <% if (!user) {
        %>
        <div>
          <a href="/auth/login" class="btn btn-primary btn-lg" role="button"
            >Login</a
          >
          <a href="/auth/signup" class="btn btn-secondary btn-lg" role="button"
            >Signup</a
          >
        </div>
        <% } %>
      </div>

      <%- include('footer') %>
    </div>
  </body>
</html>
